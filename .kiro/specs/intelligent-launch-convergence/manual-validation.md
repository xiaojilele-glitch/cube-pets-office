# 手动验证记录：intelligent-launch-convergence

## 运行入口

- 本地预览地址：`http://localhost:3002/`
- 推荐验证页面：
  - 办公室驾驶舱
  - `/tasks` 任务页

## 验证范围

- 统一智能发起入口是否已替代双入口主心智
- 四条主路径是否闭环：
  - 普通任务
  - 需澄清任务
  - 带附件任务
  - 需高级执行任务

## 验证前准备

- 确认页面已加载出统一输入框，而不是“任务命令 / 高级发起”双按钮主入口
- 确认当前运行模式显示正常：
  - `前端预览`
  - 或 `高级执行`
- 如需测试附件路径，准备一个小体积文本文件，例如 `brief.md`

## Case 1：普通任务输入

### 输入建议

```text
本周内重构支付模块，要求零停机和可回滚，并给出验收标准与交付结果。
```

### 预期结果

- 统一入口显示“系统判断：快速任务”
- 提交按钮文案显示“创建任务”
- 提交后直接创建 mission
- 焦点自动落到新任务
- 办公室右侧或任务页详情区同步切到新任务

### 记录

- 结果：`待验证`
- 备注：

## Case 2：需澄清任务输入

### 输入建议

```text
帮我推进这个任务
```

### 预期结果

- 统一入口显示“系统判断：先补问”
- 提交按钮文案显示“先澄清”
- 提交后不立即创建任务
- 输入区下方出现澄清问题面板
- 回答澄清问题后，系统继续创建 mission
- 创建成功后焦点自动切到该任务

### 记录

- 结果：`待验证`
- 备注：

## Case 3：带附件任务输入

### 输入建议

- 附件：任意 `md / txt / json` 文件 1 个
- 文本：

```text
根据附件里的需求文档和表格，先整理 brief，再拆出工作包和角色分工，最后输出交付结果和时间安排。
```

### 预期结果

- 统一入口显示“系统判断：高级编排”
- 附件区显示文件卡片、大小、类型和摘要
- 提交后进入 workflow 路径
- 如果后端立即返回 `missionId`，焦点应直接切到任务
- 如果未立即返回，则应进入“团队准备中”兼容态，后续自动回落到任务焦点

### 记录

- 结果：`待验证`
- 备注：

## Case 4：需高级执行任务输入

### 输入建议

```text
在沙盒里打开浏览器验证支付页面，抓日志并输出测试结果和回滚建议。
```

### 预期结果

- 当当前模式为 `前端预览` 时：
  - 统一入口显示“系统判断：需要高级执行”
  - 提交按钮文案显示“切到高级执行”
  - 首次提交优先触发模式升级提示或切换
- 当当前模式为 `高级执行` 时：
  - 再次提交应按正常执行路径继续

### 记录

- 结果：`待验证`
- 备注：

## 回归检查

- 办公室驾驶舱不再显示双入口主切换
- `/tasks` 页顶部命令区与办公室命令区使用同一套统一入口文案
- 页面上不再出现“任务命令 / 高级发起”作为主入口名称
- 附件添加、移除、错误提示仍能正常工作
- 澄清问题面板仍能正常提交

## 反馈模板

### 快速回填版

```text
验证页面：
验证 Case：
结果：通过 / 不通过 / 部分通过
实际现象：
预期现象：
是否可稳定复现：是 / 否
补充信息：
```

### 精细定位版

```text
验证页面：
- 办公室驾驶舱 / /tasks

验证 Case：
- Case 1 / Case 2 / Case 3 / Case 4 / 回归检查

结果：
- 通过 / 不通过 / 部分通过

输入内容：
- 粘贴你实际输入的文本

附件信息：
- 文件名 / 类型 / 大小

当前运行模式：
- 前端预览 / 高级执行

实际现象：
- 例如：按钮文案不对、没有出现澄清面板、创建后没有自动聚焦、附件卡片缺摘要

预期现象：
- 对照本文件中对应 Case 的“预期结果”

复现步骤：
1. 打开哪个页面
2. 输入什么内容
3. 点击什么按钮
4. 出现了什么问题

稳定性：
- 每次都复现 / 偶发 / 只出现一次

控制台 / Toast / 页面提示：
- 如果有报错或提示，请原样贴出

截图建议：
- 输入前
- 提交后
- 出现异常的状态
```

## 建议回传方式

- 如果只有 1 个点有问题，直接用“快速回填版”
- 如果是交互链路问题，优先用“精细定位版”
- 如果你能附上：
  - 当前页面路径
  - 当前运行模式
  - 输入文本
  - 提交后页面截图
  我基本就能直接开始修

## 结论

- 总体结果：`待验证`
- 阻塞项：
- 后续修正建议：
